{% extends 'admin/base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

<div style = "position:relative; left:70px; top:20px;">
       {# {% for post in posts %}
        {% if post.active == "Ouvert" %}#}
    <table class="table">
        <tbody>
            <tr>
                <th>IdPost</th>
                <td>{{ post.idPost }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ post.date ? post.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>UrlImg</th>
                <td><div style = "position:relative; left:0px;"  class="zoom-in-out-box"><img src="{{ asset('uploads/' ~post.urlImg) }}" style=" width:500px; height:350px; " alt=""/></div></td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ post.titre }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ post.active }}</td>
            </tr>
             <tr>
             
               {# <th>commentaire</th>
                
                <td> <p class="blog-caption">{{ post.contenu| striptags | slice(0, 100) }} ....</p> /td> #}
            </tr>
        </tbody>
    </table>
  {#{% if blog_client.author == app.user %}
                    <a > {{ include('post/_delete_form.html.twig') }}</a>
                {% endif %} #}
    <a href="{{ path('app_post_index') }}">back to list</a>

    <a href="{{ path('app_post_edit', {'idPost': post.idPost}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}

   {#{% endif %}


        {% endfor %}#}

 
{% endblock %}
