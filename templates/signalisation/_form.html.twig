

<div class="container">

 {{ form_start(form, {'attr': {novalidate: 'novalidate'}}) }}
            <div class="form-group mb-3">
                <label for="description">Description</label>
                 {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                    
                 {{ form_errors(form.description, {'attr': {'class': 'text-danger'}}) }}
                     
            </div>
            <div id="mapid" style="height: 400px;"></div>
            <div class="form-group">
                <label for="lieu">Lieu Signalisation</label>
                  {{ form_widget(form.adresse.lat, { 'attr': {'class':'lat form-control '} }) }}
                  {{ form_widget(form.adresse.lon, { 'attr': {'class':'lng form-control mb-3 mt-3'} }) }}
                 

            </div>
    
  

                        <div class="form-group">

                            <div class="col-sm-12">
                                <div class="card-box">
                                  <label for="Image">Image</label>
                                    

                  
                                    {{ form_widget(form.urlphoto, {'attr': {'class': 'form-control'}}) }}
                                          {{ form_errors(form.urlphoto, {'attr': {'class': 'text-danger'}}) }}

                                </div>
                            </div>
                        </div>

                         <div class="form-group text-right m-b-0">
                   {{ form_widget(form.Ajouter, {'attr': {'class': 'btn btn-success mt-2 text-white'}}) }}

                            <button type="reset" class="btn btn-secondary mt-2 ">reset</button>

                        </div>
  {{ form_end(form) }}
  
</div>
{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/leaflet@1.7.1/dist/leaflet.js"></script>
<script>
     var map = L.map('mapid').setView([51.505, -0.09], 13);
   
L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    maxZoom: 19,
}).addTo(map);
const markerLayer = L.marker([0, 0]).addTo(map);

try {
    window.onload = function() {
   map.on('click', function(e) {
    let pos=e.latlng;
    console.log(e);
    console.log(pos);
   markerLayer.setLatLng(e.latlng);
   document.querySelector(".lat").value=pos.lat;
   document.querySelector(".lng").value=pos.lng;
   
});
};
   
} catch (error) {
    console.error(error);
}








</script>
{% endblock %}
