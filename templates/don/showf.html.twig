{% extends 'front.html.twig' %}

{% block title %}Don{% endblock %}

{% block body %}
    <h1>Don</h1>
<div style = "position:relative; left:500px; top:200px;">
    <table class="table">
        <tbody>
            <tr>
                <th>IdDons</th>
                <td>{{ don.idDons }}</td>
            </tr>
            <tr>
                <th>TypeDons</th>
                <td>{{ don.typeDons }}</td>
            </tr>
            <tr>
                <th>DescriptionDons</th>
                <td>{{ don.descriptionDons }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_don_index') }}">back to list</a>

    <a href="{{ path('app_don_edit', {'idDons': don.idDons}) }}">edit</a>

    {{ include('don/_delete_form.html.twig') }}
{% endblock %}
