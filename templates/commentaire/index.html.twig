{% extends 'admin/base.html.twig' %}

{% block title %}Commentaire list{% endblock %}

{% block body %}

    <h1>Commentaire index</h1>

    <table class="table">
        <thead>
            <tr>
              
                <th>Contenu</th>
                <th>Date</th>
                
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                
                <td>{{ commentaire.contenu }}</td>
                <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
               {# <td>{{ commentaire.idPost.titre}} 
                <img class="card-img-top" src="{{ asset('uploads/' ~commentaire.idPost.urlImg) }}" alt="">#}
                   </td>
                <td>
                    <a href="{{ path('app_commentaire_show', {'idComntr': commentaire.idComntr}) }}">show</a>
                    <a href="{{ path('app_commentaire_edit', {'idComntr': commentaire.idComntr}) }}">edit</a>
                      <a href="{{ path('app_commentaire_delete', {'idComntr': commentaire.idComntr}) }}">delete</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_new') }}">Create new</a>
     <a href="{{ path('generator_service') }}"><button type="button" class="btn btn-primary btn-sm">PDF</button></a>
{% endblock %}


