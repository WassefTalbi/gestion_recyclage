{% extends 'client/front.html.twig' %}

{% block title %}Post index{% endblock %}

{% block body %}

<div style = "position:relative; left:350px; top:70px;">
    <h1>postes</h1>

        {% for post in posts %}
        <section class="page-section" id="services">
            <div class="container">
                
                <div class="row text-center">
                    <div class="col-md-4">
                        <span class="fa-stack fa-4x">
                            <i class=""></i>
                            
              
<td><div style = "position:relative; left:0px;"  class="zoom-in-out-box"><img src="{{ asset('uploads/' ~post.urlImg) }}" style=" width:500px; height:350px; " alt=""/></div></td>
                                       </span>
                    </div>

                    <div class="col-md-8">
                    <div style = "position:relative; top:50px; left:200px;">
            
                <h1> Description :{{ post.description }}</h1>
                <h1> Date :{{ post.date ? post.date|date('Y-m-d') : '' }}</td>
                 <h1> Titre : {{ post.titre }}</h1>
                <h1>
                    <a href="{{ path('app_post_showfront', {'idPost': post.idPost}) }}">show</a>
                    <a href="{{ path('app_post_editfront', {'idPost': post.idPost}) }}">edit</a>
                </h1>

</div>
                </div>
               
                
            </div>
        </section>
                    <h1><a href="{{ path('app_commentaire_newfront') }}">Commenter</a></h1>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
</div>
</div>
    </ruby>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}

    <a href="{{ path('app_post_newfront') }}">Create new</a>
 

 
{% endblock %}
